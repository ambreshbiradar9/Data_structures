#include<iostream>
#include<vector>
#include<queue>
#include<pair>

using namespace std;    
int main()
{
    priority_queue<int> num;
    int pp,proc;
    cin>>pp>>proc;
    vector<int> proces;
    while(proc--)
    {
        int temp;
        cin>>temp;
        proces.push_back(temp);
    }

    vector<int> time_r(pp,0);

    priority_queue

    for(int j=0;j<num_workers_;j++)
    {
      
    }

    for(int j=0;j<jobs_.size();j++)
    {
      
    }
    return 0;
}